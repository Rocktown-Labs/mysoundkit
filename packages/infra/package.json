{
  "name": "@soundkit/infra",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "alchemy dev",
    "deploy": "alchemy deploy --stage prod",
    "deploy:stage": "alchemy deploy",
    "destroy": "alchemy destroy --stage prod",
    "destroy:stage": "alchemy destroy",
    "realtimekit:presets": "bun realtimekit-presets.ts",
    "realtimekit:presets:dry-run": "bun realtimekit-presets.ts --dry-run"
  },
  "dependencies": {
    "dotenv": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@soundkit/config": "workspace:*",
    "alchemy": "catalog:",
    "typescript": "^5"
  }
}
