import { NativePlaceholderScreen } from "@/components/native-placeholder-screen";

export default function StreamsScreen() {
  return (
    <NativePlaceholderScreen
      description="Find creator broadcasts happening now on SoundKit."
      route="/live/streams"
      section="Live"
      sectionHref="/live"
      title="Streams"
    />
  );
}
